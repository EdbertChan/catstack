User: "Walk me through the full architecture of the auth flow, I want
the technical detail."

This should NOT trigger the brevity cap: the user explicitly asked for
full depth in the same turn. diu's shape rules (numbered steps, no
preamble, matter-of-fact tone) still apply, but the 150-word / 40-word
ELI5 cap does not override an explicit request for depth.
