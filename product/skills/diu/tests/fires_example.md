User: "eli5 why is the deploy failing?"

This should fire: a literal ELI5 request overrides everything else —
answer the open question from context, under 40 words, plain language,
lead with the outcome, no unexplained jargon.
