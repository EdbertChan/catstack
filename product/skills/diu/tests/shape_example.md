Agent drafts a PR title: "The hook that catches admitted wrong checks no
longer queues a check it can never deliver when a helper agent stops."

This should fire the Part 2 shape rule (always on, no brevity trigger
needed): the sentence stacks "no longer" on "never" and hides the actual
behavior under two negatives plus a trailing clause. Rewrite it as the
positive action before sending, e.g. "The hook that catches admitted wrong
checks skips queueing a check once the helper agent has stopped."
