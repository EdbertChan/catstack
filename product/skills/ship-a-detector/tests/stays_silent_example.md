User says: "The `diu-stop` README says the word limit is 200. It's 150 —
fix the number."

This is a one-line documentation correction inside a hook directory. No
detector logic changes, no pattern is added or widened, no new surface is
chosen, and nothing needs installing, wiring, or an inventory row. Working
a 20-step authoring list — pasted payloads, near-miss enumeration, fail
direction, fail-before/pass-after, install and settings-merge assertions —
would be pure overhead for a change that alters no behavior at all.

Touching a path under `engine/hooks/` is not the trigger. Changing what a
detector matches is.
