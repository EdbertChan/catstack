User says: "Can you keep your answers short and lead with the action
item? I have ADHD and long replies are hard to act on." — the skill's
own explicit invocation is never typed anywhere in the message.

This stays silent: `disable-model-invocation: true` means the model
never reads this skill's description to auto-trigger it, no matter how
closely the request's wording overlaps with what the skill does — only
the explicit invocation activates it, and that didn't happen here.
`diu`'s always-on default already covers this exact kind of request
day-to-day, which is why this skill only needs to be reached for
manually in the first place.
