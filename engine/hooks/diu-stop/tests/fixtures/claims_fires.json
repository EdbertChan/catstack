[
  {
    "label": "root cause, plain prose",
    "reply": "Found the root cause. Waiting on lens agents to finish.",
    "hit": "root cause"
  },
  {
    "label": "caused by, backticked name in the same paragraph",
    "reply": "The verbatim-repeat that triggered `intervention-must-automate` was caused by an OAuth 401 token expiry, not by the agent ignoring a named constraint.",
    "hit": "caused by"
  },
  {
    "label": "the cause was, backticked commit in the same paragraph",
    "reply": "All six CI failures, reproduced on 3.12. The cause was the code at `<sha>`.",
    "hit": "the cause was"
  },
  {
    "label": "that's why, bold list item",
    "reply": "**2. That's why it looks brilliant** — you review after the close, and every arrow sits on a real turn that wasn't on screen at the time.",
    "hit": "that's why"
  },
  {
    "label": "that is why, plain prose",
    "reply": "All three prose channels are dark. That is why the principle ships with the hook (Backlog B1), not alone.",
    "hit": "that is why"
  },
  {
    "label": "which is why, plain prose",
    "reply": "One caveat on that table: those forward returns are **not direction-adjusted** — U1 and D1 events are pooled, which is why they're all ~0.005%. They say nothing about edge yet. The ablation task, still running, is the one that answers that.",
    "hit": "which is why"
  },
  {
    "label": "this is why, plain prose",
    "reply": "Deploy exited 0 but **the live path is broken** — this is why I checked. Three findings, and one of them is my own measurement bug.",
    "hit": "this is why"
  },
  {
    "label": "the bug is, plain prose",
    "reply": "The catstack merge already worked. The bug is in Invoker.",
    "hit": "the bug is"
  },
  {
    "label": "the issue is, backticked flag and regex in the same paragraph",
    "reply": "The issue is the `re.I` flag with `(?<![0-9A-Za-z])` lookbehind — the `re.I` flag is being applied to the whole regex but the character classes already have both cases. Let me write the script to a file instead to avoid quoting issues.",
    "hit": "the issue is"
  },
  {
    "label": "the problem is, backticked name in the same paragraph",
    "reply": "Wait, actually the problem is that `user_msgs` already has a specific format `(ordinal, ts, text)` and adding a 4th element breaks the unpacking at line 195. Let me think differently.",
    "hit": "the problem is"
  },
  {
    "label": "the failure was, plain prose",
    "reply": "Specific and correct. No bug there. The failure was mine — I reworded instead of asking whether the rule was right.",
    "hit": "the failure was"
  },
  {
    "label": "because, backticked tool name is not output",
    "reply": "That defaulted to one because `Artifact` is one-file-one-call.",
    "hit": "because"
  },
  {
    "label": "because, backticked shell fragment is not output",
    "reply": "First attempt silently failed because I chained `&& echo`, which hid the error; caught it by verifying state rather than trusting the command.",
    "hit": "because"
  },
  {
    "label": "because, backticked assignment is not output",
    "reply": "**2,000 of 400,156 done, 0 mismatches.** Log looks stalled only because `PROGRESS_EVERY = 2000`.",
    "hit": "because"
  },
  {
    "label": "which is why, the message's only fence holds a command, not output",
    "reply": "Both commands are in, and tools are **still** blocked. I also can't read the hook to diagnose it — the hook blocks that read too.\n\n**Run this and paste the output:**\n\n```\n! cat ~/.claude/hooks/scope-lock/claude_pretool_scope.py\n```\n\nThat shows me what actually clears the lock. Right now I'm guessing, and guessing is what got us here.\n\nWorth noting for the findings: a gate that blocks the read of its own source can't be diagnosed from inside the session it's gating — same shape as the `disable-model-invocation` rule already in your corpus, which is why that rule says the fix must live outside the thing it protects.",
    "hit": "which is why"
  }
]
