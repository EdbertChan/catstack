{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 $HOME/.claude/hooks/auto-pr/claude_stop_autopr.py",
            "timeout": 30
          }
        ]
      }
    ]
  },
  "subagent_stop": {
    "inherit": false,
    "reason": "the open-a-PR instruction is for the session owner; a subagent shares the parent's cwd and would consume the once-per-diff marker so the parent is never told"
  }
}
