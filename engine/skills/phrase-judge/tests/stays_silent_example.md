User: "Parse the `transcript_path` JSON field from the Claude hook payload."

This should NOT fire: parsing a fixed machine field is a regex or structured
parser job, not a phrase dictionary for meaning-based prose.
