User: "Can you fix this one null-pointer bug in checkout.py?"

This should NOT fire: a one-off code fix with no working-style signal —
no restated constraint, no repeated complaint type, no heavy-involvement
pattern across a session. `automate-me` is about mining durable personal
preferences, not doing a single bounded task.
