Grading board result: "PASS — all checks green."

This should NOT fire: the sequence only triggers on a FAIL / NEEDS_WORK
result, the literal `/thrash` command, or a same-type-complaint /
`intervention-must-automate` signal — a clean PASS gives nothing to
reflect on or automate a catch for.
