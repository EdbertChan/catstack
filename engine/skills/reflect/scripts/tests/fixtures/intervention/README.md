# Intervention cluster fixtures

Synthetic Claude-shaped JSONL for `cluster_interventions` / session-mine tests.
Not real transcripts.

| File | Story |
| --- | --- |
| `make_pr_yes.jsonl` | User asks to make a PR (positive circumstance). |
| `make_pr_no.jsonl` | User says don't make a PR yet (negative circumstance). |
