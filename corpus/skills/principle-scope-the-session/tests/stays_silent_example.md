A user is mid-session on a checkout-flow bug fix. After the fix lands,
they explicitly say "great, now also add a test for the discount-code
path while we're here" — a related, expanding, but user-directed request
within the same feature area.

Does not trigger: the human is steering each expansion and confirming it
as it arrives. The skill explicitly distinguishes this normal
collaborative pattern from silent, agent-discovered scope drift.
