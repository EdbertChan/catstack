User: "What does this one function, `computeLateFee`, return for an invoice
that's 10 days overdue?"

A single, self-contained function with one clear numeric output, no second
scorer in the picture, and no provenance ambiguity between two ranking
systems. There's nothing to disambiguate between named scorers here. The
skill should stay silent.
