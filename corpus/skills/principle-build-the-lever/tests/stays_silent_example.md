The task is: "fix this one typo in the README — 'recieve' should be
'receive'."

This skill should NOT fire: this is a couple of obvious edits visible at a
glance, exactly the kind of genuinely trivial task the skill explicitly
says to skip. Building a script to fix one typo would be overhead with no
payoff — there's no repetition and no reviewability gap a lever would
close.
