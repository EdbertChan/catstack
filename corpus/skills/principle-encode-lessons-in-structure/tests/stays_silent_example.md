A user asks, for the first time, "can you rename this variable from `x` to
`userCount` for clarity?" — a one-off, one-time naming preference with no
prior instance.

This skill should NOT fire: nothing recurring has happened yet. A single
first-time request isn't a pattern to encode structurally — the skill
explicitly triggers on catching yourself repeating the *same* instruction
a second time, not on any individual piece of feedback.
