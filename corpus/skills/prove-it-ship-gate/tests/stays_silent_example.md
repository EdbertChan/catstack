User: "I fixed the typo in the README and updated the local CHANGELOG
entry — done."

This should NOT fire: the claim is "done," but the Goal has no live side
effect (no Linear ticket, deploy, external API, or live mine touched) —
it's a local file edit. Nothing here needs live evidence beyond the diff
itself.
