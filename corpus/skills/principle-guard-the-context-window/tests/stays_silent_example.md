The agent reads a single 40-line config file to check one setting, then
moves on to editing it.

A small, targeted, one-time read of a file that's actually needed for the
task is normal, necessary context — not context bloat. Nothing here is a
200KB+ payload, a 3rd redundant read, or an oversized skill load. The skill
should stay silent.
