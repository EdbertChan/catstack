The agent runs a short unit test command that prints ten lines of output and no
large log, repeated read, or explicit `/principle-guard-the-context-window`
invocation is present.

This skill stays silent: the output is already small enough for the main thread,
and no CI-log artifact workflow is needed.
