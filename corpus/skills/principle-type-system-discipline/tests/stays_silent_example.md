Writing a Python script that shells out to `git` and parses plain-text
stdout with regexes — no static type system is involved at any point in
the pipeline.

Does not trigger: the skill applies to designing types and reviewing
signatures in statically-typed languages. There's no type checker here
to exploit as a proof assistant, so none of the patterns (illegal
states, branding, exhaustive matching) have anything to attach to.
